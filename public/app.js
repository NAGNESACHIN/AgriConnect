const slotGrid=document.getElementById('slotGrid'),refreshBtn=document.getElementById('refreshBtn');const token=()=>localStorage.getItem('agriconnect_token');const escapeHtml=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function loadSlots(){slotGrid.innerHTML='<div class="loading">Loading procurement centres…</div>';try{const response=await fetch('/api/procurement/slots');const result=await response.json();slotGrid.innerHTML=result.data.map(slot=>{const used=Math.round(slot.booked/slot.capacity*100);const limited=slot.capacity-slot.booked<15;return `<article class="slot"><div class="slot-head"><span class="eyebrow">${escapeHtml(slot.date)}</span><span class="pill ${limited?'limited':''}">${limited?'Limited':'Open'}</span></div><h3>${escapeHtml(slot.centre)}</h3><p>${escapeHtml(slot.time)} · ${slot.capacity-slot.booked} spaces remaining</p><div class="capacity"><i style="width:${Math.min(100,used)}%"></i></div><button data-slot="${escapeHtml(slot.id)}">Book ${escapeHtml(slot.time)} →</button></article>`}).join('');document.querySelectorAll('[data-slot]').forEach(b=>b.addEventListener('click',()=>bookSlot(b.dataset.slot)))}catch{slotGrid.innerHTML='<div class="loading">Could not load slots. Check that the server is running.</div>'}}
async function bookSlot(slotId){if(!token()){location.href='/auth.html';return}const farmerName=JSON.parse(localStorage.getItem('agriconnect_user')||'{}').name||'';const crop=window.prompt('Crop (e.g. wheat, rice, maize)');if(!crop)return;const quantity=window.prompt('Quantity in kg');if(!quantity||Number(quantity)<=0)return;try{const response=await fetch('/api/procurement/book',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token()}`},body:JSON.stringify({farmerName,crop,quantity:Number(quantity),slotId})});const result=await response.json();if(response.status===401){localStorage.clear();location.href='/auth.html';return}if(!response.ok)throw new Error(result.message||'Booking failed');alert(`Booking confirmed: ${result.booking.bookingId}\n${result.booking.centre}\n${result.booking.date} at ${result.booking.time}`);loadSlots()}catch(error){alert(error.message)}}
const chatForm=document.getElementById('chatForm'),chatInput=document.getElementById('chatInput'),messages=document.getElementById('messages');function addMessage(text,type){const el=document.createElement('div');el.className=`bubble ${type}`;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}
chatForm?.addEventListener('submit',async e=>{e.preventDefault();const message=chatInput.value.trim();if(!message)return;addMessage(message,'user');chatInput.value='';addMessage('Thinking…','bot');const pending=messages.lastElementChild;try{const response=await fetch('/api/ai/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message})});const result=await response.json();pending.textContent=result.reply||result.message||'I could not answer that right now.'}catch{pending.textContent='The assistant is temporarily unavailable. Please try again.'}});refreshBtn?.addEventListener('click',loadSlots);loadSlots();
